# Exclusions for `make package` (scripts/package.sh). A path is excluded when its
# top component or full relative path matches any pattern below.

# Hidden files and directories (.git, .github, .env, .env.dist, .editor-version,
# .DS_Store, .claude, .omc, .gitignore, .gitattributes, .eslintignore,
# .stylelintignore, .phpcs.xml.dist, .moodle-plugin-ci.yml, ...).
# The built editor under dist/static/ contains no dotfiles, so this is safe.
.*

# Editor source checkout, dependencies and build/dev tooling.
# NOTE: the BUILT editor under dist/static/ IS shipped.
exelearning/
vendor/
node_modules/
scripts/
research/

# Development config files
phpmd-rules.xml
phpmd.xml
codecov.yml
Makefile
blueprint.json
docker-compose.yml
Dockerfile
composer.json
composer.lock
composer.phar

# JavaScript unit-test tooling (Vitest). js/scorm_tracker.js is runtime (injected by
# view.php) and ships; these dev-only Node files have no place in an installed plugin.
package.json
package-lock.json
vitest.config.mjs
tests/js/*

# Internal documentation (README.md and CHANGELOG.md are shipped). docs/ is
# developer/maintainer material (architecture notes, audits, internal decision
# references) with no runtime role — administrators read the README and the
# plugin pages.
docs/
CLAUDE.md
AGENTS.md
DEVELOPMENT.md

# Build artifacts (coverage/ is gitignored but git ls-files -o would still pick it up)
mod_exelearning-*.zip
coverage
